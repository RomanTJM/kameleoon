# Интерактивная линейная диаграмма A/B-тестирования

Веб-приложение для визуализации статистики A/B-тестирования с использованием React, TypeScript и Recharts.

## 🚀 Демо

[Живая демонстрация на GitHub Pages](https://your-username.github.io/kameleoon/)

## 📋 Описание

Приложение отображает коэффициент конверсии (conversionRate) для различных вариантов A/B-теста в виде интерактивной линейной диаграммы. Все значения отображаются в процентах с поддержкой светлой и тёмной темы.

## 🛠 Технологический стек

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Recharts** - библиотека для построения графиков
- **Vite** - сборщик и dev-сервер
- **CSS Modules** - модульная стилизация
- **vite-plugin-svgr** - импорт SVG как React компонентов

## Реализованные функции

### Основные требования

- ✅ Линейный график коэффициента конверсии для всех вариантов
- ✅ Отображение всех значений в процентах
- ✅ Вертикальная пунктирная линия и всплывающее окно при наведении курсора
- ✅ Всегда выбран хотя бы один вариант (нельзя снять все)
- ✅ Автоматическая адаптация осей X и Y к видимому диапазону данных
- ✅ Адаптивный макет для экранов от 671px до 1300px
- ✅ Селектор вариантов (чекбоксы для выбора отображаемых линий)
- ✅ Селектор периода (День/Неделя/Месяц)

### Бонусные функции

- ✅ **Масштабирование и панорамирование**
  - Кнопка панорамирования (курсор в пунктирном квадрате)
  - Кнопки увеличения/уменьшения масштаба (+/-)
  - Кнопка сброса масштаба (круговая стрелка)
  - Диапазон масштаба: 0.5x - 3x

- ✅ **Селектор стиля линии**
  - Line (обычная линия)
  - Smooth (сглаженная линия)
  - Area (заполненная область)
  - Bands (толстая полупрозрачная обводка вокруг линии)

- ✅ **Переключение светлой/тёмной темы**
  - Светлая тема: белый фон, тёмный текст
  - Тёмная тема: тёмно-синий фон (#000027), белый текст
  - Автоматическая адаптация цветов линий и иконок

- ✅ **Экспорт диаграммы в PNG**
  - Кнопка экспорта с иконкой загрузки
  - Сохранение графика в высоком качестве
  - Автоматическое именование файла с датой


## Установка и запуск

### Предварительные требования

- Node.js 18+ 
- npm или yarn

### Локальная установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/RomanTJM/kameleoon.git
cd kameleoon
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev-сервер:
```bash
npm run dev
```

4. Откройте браузер и перейдите по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

### Предпросмотр продакшен-сборки

```bash
npm run preview
```

## Структура проекта

```
kameleoon/
├── src/
│   ├── assets/
│   │   └── icons/
│   │       ├── arrow-down.svg
│   │       ├── calendar.svg
│   │       ├── dark.svg
│   │       ├── download.svg
│   │       ├── light.svg
│   │       ├── minus.svg
│   │       ├── panning-btn.svg
│   │       ├── plus.svg
│   │       ├── refresh.svg
│   │       └── trophy.svg
│   ├── components/
│   │   ├── Chart/
│   │   │   ├── Chart.tsx
│   │   │   └── Chart.module.css
│   │   ├── Controls/
│   │   │   ├── Controls.tsx
│   │   │   └── Controls.module.css
│   │   └── CustomTooltip/
│   │       ├── CustomTooltip.tsx
│   │       └── CustomTooltip.module.css
│   ├── utils/
│   │   ├── dataProcessor.ts
│   │   └── formatters.ts
│   ├── types.ts
│   ├── App.tsx
│   ├── App.module.css
│   ├── main.tsx
│   └── index.css
├── data.json
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

##  Настройка GitHub Pages

1. Перейдите в Settings → Pages вашего репозитория
2. Выберите источник: GitHub Actions
3. При каждом push в ветку `main` будет автоматически выполняться деплой

Или используйте ручной деплой:

```bash
npm run build
# Затем загрузите содержимое папки dist/ в ветку gh-pages
```

Убедитесь, что в `vite.config.ts` указан правильный `base` путь (имя вашего репозитория).

